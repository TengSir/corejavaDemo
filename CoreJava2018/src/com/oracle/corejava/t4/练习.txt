数组做练习：


1.定义一个一维int类型数组，长度定义30个，用随机数赋值，随机数范围控制在0-100之间，

要求数组里面不能有重复的数值，然后对数组排降序输出

2.定义一个int类型数组，长度30个，里面赋值从1-30，每个数字代表一个学生对应的编号，

现在要求写一个简单的算法，每次随机从数组里抽一个数字用来安排这个编号对应的学生值日。

要求在一次轮回之前每个人只能被抽中一次，但是抽出来的顺序必须是随机的。
